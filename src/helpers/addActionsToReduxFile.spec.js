const addActionsToReduxFile = require('./addActionsToReduxFile');

describe('When the file has ExportNamedDeclaration\'s', () => {
  it('Adds the new constant exports after the old constant exports', () => {

  })
  
  it('Adds the new arrow functions after the old arrow functions', () => {

  })
})

